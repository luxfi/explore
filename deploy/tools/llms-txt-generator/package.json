{
  "name": "@luxfi/llms-txt-generator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && pnpm generate:dev",
    "build": "vite build --logLevel error",
    "generate": "node dist/index.js",
    "generate:dev": "dotenv -e ../../../configs/envs/.env.eth -- node dist/index.js"
  },
  "dependencies": {
    "dedent": "catalog:"
  },
  "devDependencies": {
    "@types/dedent": "catalog:",
    "@types/node": "catalog:",
    "dotenv-cli": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
